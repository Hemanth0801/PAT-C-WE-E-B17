#generators are function which returns a iterators(squence of items)
#this function used  yield insted of return
# in function return statement , in generators yield statement

#generator function contains yield statement -> which pauses the execution and returns the
#iterator value and then resume the next step defined inside the generator function
